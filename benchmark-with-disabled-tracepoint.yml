type: ruby_stdout
name: Optcarrot Lan_Master.nes with disabled TracePoint
command: -r./tools/shim.rb bin/optcarrot-with-disabled-tracepoint --benchmark examples/Lan_Master.nes
metrics:
  Number of frames:
    unit: fps
    from_stdout: 'Float(stdout.match(/^fps: (?<fps>\d+\.\d+)$/)[:fps])'
environment:
  Checksum:
    from_stdout: 'Integer(stdout.match(/^checksum: (?<checksum>\d+)$/)[:checksum])'
